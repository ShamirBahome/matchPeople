SECRET_KEY=change-me-in-production
DATABASE_URL=sqlite:///peer_connect.db
FLASK_ENV=development
ADMIN_EMAIL=admin@peerconnect.local
# Set to 1 locally to enable GET /dev/seed (demo data). Never enable in production.
ALLOW_SEED=1
